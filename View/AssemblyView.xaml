<UserControl x:Class="View.AssemblyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             xmlns:data="clr-namespace:ViewModel.Data;assembly=ViewModel"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <TreeView Name="TrvStructure" ItemsSource="{Binding Root}" Margin="10" TreeViewItem.Expanded="ItemExpanded">
            <!-- <b:Interaction.Triggers> -->
            <!--      <b:EventTrigger> -->
            <!--          <b:InvokeCommandAction Command="{Binding }" par></b:InvokeCommandAction> -->
            <!--      </b:EventTrigger> -->
            <!--  </b:Interaction.Triggers> -->
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate DataType="{x:Type data:TreeNode}" ItemsSource="{Binding Children}">
                    <TextBlock Text="{Binding Name}" />
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</UserControl>